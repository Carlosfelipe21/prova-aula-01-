
# Você está criando um programa para calcular a área de um retângulo. 
# O programa deve solicitar ao usuário que forneça a base e a altura do retângulo. 
# Em seguida, o programa deve calcular a área usando a fórmula:

# Área=Base×Altura



base = float(input("porfavor, informe o valor base do retâmgulo:"))
altura = float(input("porfavor, informe a altura do retângulo:"))

area = (base * altura)
print(F"calculando a Base {base} e a altura {altura} temos o resultado da area: {area}.")